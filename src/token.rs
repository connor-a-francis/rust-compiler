#[derive(Debug, Clone, PartialEq)]
pub enum Token {
    EOF,
    EOL,
    Def,
    Let,
    Extern,
    Return,
    Identifier(String),
    Number(f32),
    Add,
    Sub,
    Mult,
    Div,
    Eq,
    LParen,
    RParen,
    LBrace,
    RBrace,
    Misc(char),
}
